import styled from 'styled-components';

export const Grid = styled.div`
  display: grid;
  color: white;
  gap: 1rem;
  grid-template-columns: repeat(4, 1fr);
  position: relative;
  text-align: center;
  top: 20rem;
`;
